import React from 'react';
import Header from "../components/header";

const MovieUploader = () => {
    return (
        <div>
            <Header/>
            Movie Uploader
        </div>
    );
};

export default MovieUploader;