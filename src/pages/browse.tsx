import React from 'react';
import Header from "../components/header";

const Browse = () => {
    return (
        <div>
            <Header/>
            browse
        </div>
    );
};

export default Browse;